<h1>Hello, world</h1>

<% if Current.user %>
  <p>You are signed in as <%= Current.user.email_address %>
  <%= button_to "Sign Out", session_path, method: :delete, data: { turbo_method: :delete } %>
<% else %>
  <p>You are not signed in</p>
<% end %>
