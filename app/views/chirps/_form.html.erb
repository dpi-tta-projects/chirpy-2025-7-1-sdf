<%= form_with(model: chirp) do |form| %>
  <% if chirp.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(chirp.errors.count, "error") %> prohibited this chirp from being saved:</h2>

      <ul>
        <% chirp.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-2">
    <%= form.label(:body, class: "form-label visually-hidden") %>
    <%= form.textarea(:body, class: "form-control", placeholder: "What is happening?!") %>
  </div>

  <div class="d-flex justify-content-end">
    <%= form.submit(class: "btn btn-primary rounded-pill") %>
  </div>
<% end %>
